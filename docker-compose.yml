version: '3.8'

services:
  client:
    build: .
    container_name: land_registry_client
    restart: always
    ports:
      - "5173:80"
    networks:
      - frontend-network

networks:
  frontend-network:
    driver: bridge
